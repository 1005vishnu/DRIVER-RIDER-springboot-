# Application Name
spring.application.name=demo

# Server Configuration
#server.port=3306

# Database Configuration (MySQL)
# Use environment variables with fallback defaults
spring.datasource.url=${SPRING_DATASOURCE_URL:jdbc:mysql://mysql:3306/ridehailing}
spring.datasource.username=${SPRING_DATASOURCE_USERNAME:root}
spring.datasource.password=${SPRING_DATASOURCE_PASSWORD:root}
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

# Hibernate (JPA) Configuration
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.dialect=org.hibernate.dialect.MySQLDialect

# ---
# LOCAL DEVELOPMENT OVERRIDE (optional):
# Uncomment and use these settings for local development if you run MySQL locally.
#spring.datasource.url=jdbc:mysql://127.0.0.1:3306/ridehailing
#spring.datasource.username=root
#spring.datasource.password=
#spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver

